# zonky_model
Script to create XGBoost model to predict problematic loans from zonky.cz/risk dataset.
The data must be imported in csv format since Zonky does not have a stable version of xls export.
